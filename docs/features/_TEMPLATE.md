# <Название фичи>

> **Одна строка:** что это и зачем. Если читаешь только её — уже понятно.

## Входные точки

- Страница: `src/pages/<page>.tsx`
- Компоненты: `src/components/<Component>.tsx`
- REST: `server/routes/<route>.ts` (маршрут `/api/...`)
- MCP: `server/mcp.ts` (инструмент `<tool_name>` — если есть)
- Логика: `src/lib/<module>.ts`

## Поток данных

```
UI (React) ─► REST /api/... (или MCP-инструмент) ─► appService ─► src/lib/...
                                                      ├─► src/lib/stores/** ─► iTunes / google-play-scraper
                                                      └─► Drizzle (@/db) ─► PostgreSQL
```

(ASCII или mermaid — максимально кратко)

## Карта файлов

| Файл              | Роль                      |
| ----------------- | ------------------------- |
| `path/to/file.ts` | что делает, одной строкой |

(Указывай только **ключевые** файлы, не все.)

## API / MCP

Эндпоинты/инструменты, формат запроса/ответа.

## Хранение

Таблицы из `src/db/schema.ts`, ключевые поля и связи.

## Инварианты и подводные камни

Неочевидные правила, которые легко нарушить. То, что не видно из кода.

## Связи

- Родственная фича: [<name>](./<name>.md)
- Общая карта: [../ARCHITECTURE.md](../ARCHITECTURE.md)

---

_Обновляй этот файл при любой правке фичи (правило AGENTS.md №8). Держи тощим: ссылайся на код, не дублируй его._
